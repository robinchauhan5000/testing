import 'package:flutter/material.dart';

class MainDashBoardScreen extends StatelessWidget {
  static const route = '/MainDashBoardScreen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Main Dashboard'),
      ),
    );
  }
}
